#include <iostream>
#include <cmath>
using namespace std;

int main(){

    // Trunc zaokruzuje na cijeli broj prema nuli

    cout << trunc(5.5) << endl;
    cout << trunc(-5.5) << endl;

    system("PAUSE");
    return 0;
}